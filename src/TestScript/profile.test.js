const delele = require("../components/User/Profile/profile/delete");

// eslint-disable-next-line no-undef
test("Good delete jobs", () => {
  // eslint-disable-next-line no-undef
  expect(delele(1).toBe("text"));
});
